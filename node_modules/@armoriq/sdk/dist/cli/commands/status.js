"use strict";
/**
 * armoriq status / logs — local-only commands that read the cached state
 * and CLI log file. Mirrors armoriq_sdk/cli.py:cmd_status / cmd_logs.
 */
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.cmdStatus = cmdStatus;
exports.cmdLogs = cmdLogs;
const fs = __importStar(require("fs"));
const util_1 = require("../util");
const state_1 = require("../state");
function cmdStatus() {
    const state = (0, state_1.loadState)();
    if (!state || Object.keys(state).length === 0) {
        (0, util_1.out)('No local registration state found. Run `armoriq register` first.');
        return 1;
    }
    (0, util_1.out)(`Agent: ${state.agent_id ?? 'unknown'}`);
    (0, util_1.out)(`User: ${state.user_id ?? 'unknown'}`);
    (0, util_1.out)(`Environment: ${state.environment ?? 'unknown'}`);
    (0, util_1.out)(`Registered at: ${state.registered_at ?? 'unknown'}`);
    (0, util_1.out)(`Proxy endpoint: ${state.proxy_endpoint ?? 'unknown'}`);
    const mcpServers = state.mcp_servers ?? [];
    (0, util_1.out)(`MCP servers: ${mcpServers.length > 0 ? mcpServers.join(', ') : '(none)'}`);
    return 0;
}
function cmdLogs(args) {
    if (!fs.existsSync(state_1.LOG_FILE)) {
        (0, util_1.out)('No CLI logs found yet.');
        return 0;
    }
    const tailFile = (offset) => {
        const stat = fs.statSync(state_1.LOG_FILE);
        if (stat.size <= offset)
            return offset;
        const fd = fs.openSync(state_1.LOG_FILE, 'r');
        const buf = Buffer.alloc(stat.size - offset);
        fs.readSync(fd, buf, 0, buf.length, offset);
        fs.closeSync(fd);
        for (const line of buf.toString('utf-8').split('\n')) {
            const trimmed = line.trim();
            if (!trimmed)
                continue;
            try {
                const event = JSON.parse(trimmed);
                (0, util_1.out)(`[${event.timestamp ?? ''}] ${event.event ?? ''}: ${JSON.stringify(event.details ?? {})}`);
            }
            catch {
                (0, util_1.out)(trimmed);
            }
        }
        return stat.size;
    };
    let offset = tailFile(0);
    if (!args.follow)
        return 0;
    // Simple poll loop for --follow; ctrl-C exits.
    const interval = setInterval(() => {
        offset = tailFile(offset);
    }, 1000);
    // Return a never-resolving promise via process exit hook isn't useful here;
    // the user expects --follow to block. Keep the process alive until SIGINT.
    process.on('SIGINT', () => {
        clearInterval(interval);
        process.exit(0);
    });
    // Block forever — this function is the entrypoint of the command.
    return 0;
}
//# sourceMappingURL=status.js.map