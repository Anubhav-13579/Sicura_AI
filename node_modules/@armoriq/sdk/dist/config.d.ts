/**
 * armoriq.yaml configuration models and loader.
 *
 * Mirrors armoriq_sdk/config.py shape so the same file is portable
 * between the two SDKs.
 */
export declare const DEFAULT_PROXY_URL = "https://customer-proxy.armoriq.ai";
export declare class ArmorIQConfigError extends Error {
    constructor(message: string);
}
/**
 * Resolve `$ENV_VAR` or `${ENV_VAR}` references against process.env.
 * Returns the original string when it is not an env reference.
 */
export declare function resolveEnvReference(value: string): string;
export interface IdentityConfig {
    api_key: string;
    user_id: string;
    agent_id: string;
}
export interface ProxyConfig {
    url: string;
    timeout: number;
    max_retries: number;
}
export type MCPAuthType = 'none' | 'bearer' | 'api_key';
export interface MCPAuthConfig {
    type: MCPAuthType;
    token?: string;
    api_key?: string;
}
export interface MCPServerConfig {
    id: string;
    url: string;
    description?: string;
    auth: MCPAuthConfig;
}
export interface PolicyConfig {
    allow: string[];
    deny: string[];
}
export interface IntentConfig {
    ttl_seconds: number;
    require_csrg: boolean;
}
export type Environment = 'sandbox' | 'production';
export interface ArmorIQConfig {
    version: 'v1';
    identity: IdentityConfig;
    environment: Environment;
    proxy: ProxyConfig;
    mcp_servers: MCPServerConfig[];
    policy: PolicyConfig;
    intent: IntentConfig;
}
export declare function parseArmorIQConfig(raw: any): ArmorIQConfig;
export declare function loadArmorIQConfig(filePath?: string): ArmorIQConfig;
/**
 * Resolve every `$VAR` reference in the config against process.env.
 * Returns a new config with the references replaced by their values.
 */
export declare function resolveEnvReferences(config: ArmorIQConfig): ArmorIQConfig;
export declare function saveArmorIQConfig(config: ArmorIQConfig, filePath?: string): void;
//# sourceMappingURL=config.d.ts.map